services:
  - type: web
    name: studioagent
    runtime: node
    buildCommand: cd 03-mvp/frontend && npm install && npm run build
    startCommand: cd 03-mvp/frontend && npm start
    envVars:
      - key: NODE_VERSION
        value: 20
      - key: NODE_ENV
        value: production
    plan: free
